// Help entries for the Symbolic Math Toolbox, extracted from symbolic.ts.
// Keyed by function name; consumed via ToolboxModule.help (see tb/types.ts).
import type { HelpEntry } from './types';

export const HELP_SYMBOLIC: Record<string, HelpEntry | string> = {
    argnames: { summary: 'Return input variables of a symbolic function', syntax: ['args = argnames(f)'], description: ['args = argnames(f) returns the input variables of the symbolic function or matrix function f.', 'Use formula(f) to get the expression body.'], seealso: ['formula', 'syms', 'symvar', 'symfun'] },
    bernstein: { summary: 'Bernstein polynomial approximation', syntax: ['bernstein(f,n,t)', 'bernstein(g,n,t)', 'bernstein(g,var,n,t)'], description: ['bernstein(f,n,t) returns the n-th order Bernstein polynomial approximation of f evaluated at t.', 'bernstein(g,n,t) with a symbolic expression g regards g as a univariate function of its default variable.'], seealso: ['bernsteinMatrix', 'symsum', 'nchoosek'] },
    cell2sym: { summary: 'Convert cell array to symbolic array', syntax: ['S = cell2sym(C)', 'S = cell2sym(C,flag)'], description: ['S = cell2sym(C) converts a cell array C to a symbolic array S; each element of C must be convertible to a symbolic object.', 'If elements are scalars, size(S) == size(C) and S(k) == sym(C(k)).'], seealso: ['sym2cell', 'cell2mat', 'num2cell'] },
    charToFunction: { summary: 'Convert function name string to function handle', syntax: ['f = charToFunction(name)'], description: ['charToFunction creates a function handle from a character vector name, used internally by the Symbolic Toolbox.'], seealso: ['str2sym', 'matlabFunction'] },
    dsolve: { summary: 'Solve ordinary differential equations symbolically', syntax: ['S = dsolve(eqn)', 'S = dsolve(eqn,cond)', 'S = dsolve(eqn,cond,Name,Value)'], description: ['S = dsolve(eqn) symbolically solves the ODE expressed with diff; cond specifies initial/boundary conditions.', 'Returns a symbolic expression for unique solutions, or a struct for systems of ODEs.'], seealso: ['odeToVectorField', 'simplify', 'solve'] },
    eliminate: { summary: 'Eliminate variables from a system of polynomial equations', syntax: ['expr = eliminate(eqns,vars)'], description: ['expr = eliminate(eqns,vars) eliminates vars from the rational equations eqns, returning expressions equal to zero.', 'Useful for reducing a system before solving.'], seealso: ['gbasis', 'solve'] },
    findSymType: { summary: 'Find subexpressions of a given symbolic type', syntax: ['X = findSymType(symObj,type)', 'X = findSymType(symObj,funType,vars)'], description: ['X = findSymType(symObj,type) returns a vector of symbolic subobjects of the given type from symObj.', 'Returns empty if no subobject of that type exists.'], seealso: ['hasSymType', 'isSymType', 'symType'] },
    fourier: { summary: 'Symbolic Fourier transform', syntax: ['FT = fourier(f)', 'FT = fourier(f,transVar)', 'FT = fourier(f,var,transVar)'], description: ['FT = fourier(f) returns the Fourier transform of f with default independent variable t and transform variable w.', 'FT = fourier(f,transVar) uses transVar instead of w.'], seealso: ['ifourier', 'laplace', 'ztrans'] },
    functionalDerivative: { summary: 'Functional derivative of a symbolic functional', syntax: ['G = functionalDerivative(f,y)'], description: ['G = functionalDerivative(f,y) returns the functional derivative δS/δy(x) of S[y]=∫f(x,y,y\',...) dx.', 'If y is a vector of symbolic functions, returns a vector of functional derivatives.'], seealso: ['diff', 'dsolve', 'int'] },
    has: { summary: 'Check if symbolic expression contains a subexpression', syntax: ['has(expr,subexpr)'], description: ['has(expr,subexpr) returns true if expr contains subexpr as a subexpression.', 'If expr is an array, returns an array of the same size with per-element results.'], seealso: ['subs', 'subexpr', 'hasSymType'] },
    hasSymType: { summary: 'Check if expression contains a specific symbolic type', syntax: ['tf = hasSymType(expr,type)'], description: ['Returns true if expr contains any subexpression of the given symbolic type (e.g. "variable", "constant").'], seealso: ['isSymType', 'findSymType', 'symType'] },
    htrans: { summary: 'Symbolic Hilbert transform', syntax: ['F = htrans(f,x)'], description: ['F = htrans(f,x) computes the Hilbert transform of f with respect to x.', 'The inverse is ihtrans.'], seealso: ['ihtrans', 'fourier'] },
    hypergeom: { summary: 'Generalized hypergeometric function', syntax: ['F = hypergeom(n,d,z)'], description: ['F = hypergeom(n,d,z) computes the generalized hypergeometric function pFq(n;d;z).', 'Converges for |z|<1 when length(n)<=length(d). Many special functions are special cases.'], seealso: ['int', 'series'] },
    ifourier: { summary: 'Symbolic inverse Fourier transform', syntax: ['ifourier(F)', 'ifourier(F,transVar)', 'ifourier(F,var,transVar)'], description: ['ifourier(F) returns the inverse Fourier transform of F with default variable w and result in x.', 'ifourier(F,transVar) uses transVar instead of x.'], seealso: ['fourier', 'ilaplace', 'iztrans'] },
    ihtrans: { summary: 'Symbolic inverse Hilbert transform', syntax: ['f = ihtrans(H)', 'f = ihtrans(H,transVar)', 'f = ihtrans(H,var,transVar)'], description: ['f = ihtrans(H) returns the inverse Hilbert transform of H with default variable x and result in t.'], seealso: ['htrans', 'fourier', 'ifourier'] },
    ilaplace: { summary: 'Symbolic inverse Laplace transform', syntax: ['f = ilaplace(F)', 'f = ilaplace(F,transVar)', 'f = ilaplace(F,var,transVar)'], description: ['f = ilaplace(F) returns the inverse Laplace transform of F with default variable s and result in t.', 'f = ilaplace(F,transVar) uses transVar instead of t.'], seealso: ['laplace', 'fourier', 'iztrans', 'ztrans'] },
    isCondition: { summary: 'Check if symbolic expression is a relational condition', syntax: ['tf = isCondition(expr)'], description: ['Returns true if expr is a symbolic relation (==, ~=, <, <=, >, >=) or logical combination thereof.'], seealso: ['isAlways', 'isSymType'] },
    isDistinctVariable: { summary: 'Check if symbolic variables are distinct', syntax: ['tf = isDistinctVariable(x,y)'], description: ['Returns true if x and y are distinct symbolic variables (different symbol objects).'], seealso: ['symvar', 'isVariable'] },
    isSymType: { summary: 'Check if symbolic object is of a given type', syntax: ['TF = isSymType(symObj,type)', 'TF = isSymType(symObj,funType,vars)'], description: ['TF = isSymType(symObj,type) returns true if symObj has type as its topmost operator type.', 'type must be a case-sensitive string such as "variable", "constant", "plus", "times", "sin".'], seealso: ['hasSymType', 'findSymType', 'symType'] },
    isVariable: { summary: 'Check if symbolic expression is a single variable', syntax: ['tf = isVariable(expr)'], description: ['Returns true if expr is a single symbolic variable (not a compound expression or constant).'], seealso: ['symvar', 'isDistinctVariable', 'isSymType'] },
    iztrans: { summary: 'Symbolic inverse Z-transform', syntax: ['iztrans(F)', 'iztrans(F,transVar)', 'iztrans(F,var,transVar)'], description: ['iztrans(F) returns the inverse Z-transform of F with default variable z and result in n.', 'iztrans(F,transVar) uses transVar instead of n.'], seealso: ['ztrans', 'laplace', 'ilaplace'] },
    laplace: { summary: 'Symbolic Laplace transform', syntax: ['F = laplace(f)', 'F = laplace(f,transVar)', 'F = laplace(f,var,transVar)'], description: ['F = laplace(f) returns the Laplace transform of f with default variable t and transform variable s.', 'F = laplace(f,transVar) uses transVar instead of s.'], seealso: ['ilaplace', 'fourier', 'ztrans'] },
    mapSymType: { summary: 'Apply function to subexpressions of a given symbolic type', syntax: ['X = mapSymType(symObj,type,func)', 'X = mapSymType(symObj,funType,vars,func)'], description: ['X = mapSymType(symObj,type,func) applies func to all subobjects of type type in symObj.', 'func must be a function handle or a symbolic function.'], seealso: ['findSymType', 'isSymType', 'symType'] },
    matlabFunction: { summary: 'Convert symbolic expression to MATLAB function handle', syntax: ['ht = matlabFunction(f)', 'ht = matlabFunction(f1,...,fN)', 'ht = matlabFunction(___,Name,Value)'], description: ['ht = matlabFunction(f) converts symbolic expression f to a MATLAB function handle for numerical evaluation.', 'Name=Value options include Vars (input order), File (save to .m file), and Optimize.'], seealso: ['ccode', 'fortran', 'daeFunction'] },
    odeToVectorField: { summary: 'Convert higher-order ODE to first-order system', syntax: ['V = odeToVectorField(eqn1,...,eqnN)', '[V,S] = odeToVectorField(eqn1,...,eqnN)'], description: ['V = odeToVectorField(eqn) converts a higher-order ODE to a system of first-order ODEs, returned as a symbolic vector.', '[V,S] = odeToVectorField(eqn) also returns S showing the substitution variable names.'], seealso: ['dsolve', 'matlabFunction', 'ode45'] },
    pade: { summary: 'Padé approximant of a symbolic expression', syntax: ['pade(f,var)', 'pade(f,var,a)', 'pade(___,Name,Value)'], description: ['pade(f,var) returns the third-order Padé approximant of f at var=0.', 'Padé approximants often converge better than Taylor series away from the expansion point.'], seealso: ['series', 'taylor'] },
    partfrac: { summary: 'Partial fraction decomposition', syntax: ['partfrac(expr,var)', 'partfrac(expr,var,Name,Value)'], description: ['partfrac(expr,var) finds the partial fraction decomposition of expr with respect to var.', 'Useful for computing inverse Laplace/Z transforms and evaluating integrals of rational functions.'], seealso: ['children', 'coeffs', 'collect', 'ilaplace'] },
    piecewise: { summary: 'Create symbolic piecewise expression', syntax: ['pw = piecewise(cond1,val1,cond2,val2,...)', 'pw = piecewise(cond1,val1,...,otherwiseVal)'], description: ['pw = piecewise(cond1,val1,...) returns a symbolic expression whose value is val_k when cond_k is true.', 'A trailing otherwise value applies when no condition holds.'], seealso: ['heaviside', 'dirac', 'assume'] },
    poles: { summary: 'Find poles of a symbolic expression', syntax: ['P = poles(f,var)', 'P = poles(f,var,a,b)', '[P,N,R] = poles(___)'], description: ['P = poles(f,var) returns the poles of f with respect to var.', '[P,N,R] = poles(...) also returns the pole orders N and residues R.'], seealso: ['limit', 'partfrac', 'vpasolve'] },
    polynomialDegree: { summary: 'Degree of a symbolic polynomial', syntax: ['polynomialDegree(p)', 'polynomialDegree(p,vars)'], description: ['polynomialDegree(p) returns the total degree of polynomial p with respect to all its variables.', 'polynomialDegree(p,vars) returns the degree with respect to the specified variables only.'], seealso: ['coeffs', 'polynomialReduce'] },
    polynomialReduce: { summary: 'Reduce polynomial by a set of divisor polynomials', syntax: ['r = polynomialReduce(p,d)', 'r = polynomialReduce(p,d,vars)', '[r,q] = polynomialReduce(___)'], description: ['r = polynomialReduce(p,d) returns the remainder of dividing polynomial p by the set of polynomials d.', '[r,q] = polynomialReduce(...) also returns the quotient vector q.'], seealso: ['eliminate', 'gbasis', 'polynomialDegree'] },
    quorem: { summary: 'Quotient and remainder of symbolic polynomial division', syntax: ['[Q,R] = quorem(A,B,var)'], description: ['[Q,R] = quorem(A,B,var) returns quotient Q and remainder R of dividing polynomial A by B in variable var, so that A = Q*B + R.', 'For matrices, division is element-wise.'], seealso: ['deconv', 'mod', 'rem'] },
    rewrite: { summary: 'Rewrite symbolic expression in terms of another function', syntax: ['R = rewrite(expr,target)'], description: ["R = rewrite(expr,target) rewrites expr in terms of target function (e.g. 'exp', 'sin', 'log', 'heaviside').", 'The result is mathematically equivalent.'], seealso: ['simplify', 'combine', 'expand'] },
    series: { summary: 'Puiseux series expansion of a symbolic expression', syntax: ['series(f,var)', 'series(f,var,a)', 'series(___,Name,Value)'], description: ['series(f,var) returns the Puiseux series expansion of f up to the fifth order at var=0.', 'series(f,var,a) expands around var=a. Unlike taylor, series allows fractional and negative powers.'], seealso: ['pade', 'taylor', 'limit'] },
    str2sym: { summary: 'Convert string to symbolic expression', syntax: ['str2sym(symstr)'], description: ["str2sym('sin(x)+cos(x)') parses the string and returns the corresponding symbolic expression.", 'Prefer syms/sym for interactive use; str2sym is useful when reading expressions from text files.'], seealso: ['sym', 'syms', 'subs', 'vpa'] },
    subexpr: { summary: 'Rewrite expression by extracting common subexpression', syntax: ['[r,sigma] = subexpr(expr)', "[r,var] = subexpr(expr,'var')"], description: ["[r,sigma] = subexpr(expr) rewrites expr by substituting a common subexpression with the variable sigma.", 'Useful for simplifying or displaying complex expressions compactly.'], seealso: ['simplify', 'subs', 'children'] },
    sym2cell: { summary: 'Convert symbolic array to cell array', syntax: ['C = sym2cell(S)'], description: ['C = sym2cell(S) converts symbolic array S to a cell array C of the same size.', 'Each element of C is the corresponding symbolic scalar from S.'], seealso: ['cell2sym', 'cell2mat', 'num2cell'] },
    symFunType: { summary: 'Return symbolic function type identifier', syntax: ['t = symFunType(f)'], description: ['symFunType returns internal type information for a symbolic function, used by the Symbolic Toolbox type dispatch system.'], seealso: ['symType', 'isSymType', 'findSymType'] },
    symType: { summary: 'Return the symbolic type of an expression', syntax: ['s = symType(symObj)'], description: ["s = symType(symObj) returns a string describing the topmost type of symObj, e.g. 'variable', 'plus', 'sin'.", "symType(sym('x')) returns 'variable'; symType(sin(sym('x'))) returns 'sin'."], seealso: ['isSymType', 'hasSymType', 'findSymType'] },
    symfalse: { summary: 'Symbolic logical false constant', syntax: ['symfalse', 'F = symfalse(n)', 'F = symfalse(sz)'], description: ['symfalse is the symbolic counterpart of logical false, returned by symbolic comparisons that always evaluate to false.', 'symfalse(n) returns an n-by-n matrix of symfalse values.'], seealso: ['symtrue', 'isAlways', 'and', 'or', 'not'] },
    symfun: { summary: 'Create a symbolic function with explicit formula and variables', syntax: ['f = symfun(expr,vars)'], description: ['f = symfun(expr,vars) creates a symbolic function with body expr and input variables vars.', 'Equivalent to syms f(x) followed by f(x)=expr, but in one call.'], seealso: ['syms', 'sym', 'argnames', 'formula'] },
    symtrue: { summary: 'Symbolic logical true constant', syntax: ['symtrue', 'T = symtrue(n)', 'T = symtrue(sz)'], description: ['symtrue is the symbolic counterpart of logical true, returned by symbolic comparisons that always evaluate to true.', 'symtrue(n) returns an n-by-n matrix of symtrue values.'], seealso: ['symfalse', 'isAlways', 'and', 'or', 'not'] },
    texlabel: { summary: 'Convert symbolic expression to TeX string for plot labels', syntax: ['str = texlabel(expr)'], description: ['str = texlabel(expr) converts a symbolic expression to a TeX string suitable for MATLAB plot labels and titles.', 'Unlike latex, the output targets figure annotation rather than LaTeX documents.'], seealso: ['latex', 'title', 'xlabel'] },
    vpaintegral: { summary: 'Numerically integrate using variable-precision arithmetic', syntax: ['vpaintegral(f,a,b)', 'vpaintegral(f,x,a,b)', 'vpaintegral(___,Name,Value)'], description: ['vpaintegral(f,a,b) numerically approximates the integral of f from a to b using vpa arithmetic.', 'More accurate than integral for high-precision needs; slower than double-precision quadrature.'], seealso: ['int', 'integral', 'vpa', 'digits'] },
    vpasum: { summary: 'Sum series using variable-precision arithmetic', syntax: ['S = vpasum(f,x,a,b)'], description: ['vpasum(f,x,a,b) numerically evaluates the sum of f in x from a to b using vpa arithmetic.'], seealso: ['symsum', 'vpa', 'sum'] },
    ztrans: { summary: 'Symbolic Z-transform', syntax: ['fz = ztrans(f)', 'fz = ztrans(f,transVar)', 'fz = ztrans(f,var,transVar)'], description: ['fz = ztrans(f) returns the Z-transform of f with default variable n and transform variable z.', 'fz = ztrans(f,transVar) uses transVar instead of z.'], seealso: ['iztrans', 'laplace', 'fourier'] },
  };
