// Help entries for the Communications Toolbox, extracted from comm.ts.
// Keyed by function name; consumed via ToolboxModule.help (see tb/types.ts).
import type { HelpEntry } from './types';

export const HELP_COMM: Record<string, HelpEntry | string> = {
    zadoffChuSeq: { summary: 'Generate a Zadoff-Chu sequence', syntax: ['seq = zadoffChuSeq(R,N)'], description: ['seq = zadoffChuSeq(R,N) generates the Rth root Zadoff-Chu sequence of length N, as defined in 3GPP TS 36.211.', 'The sequence is defined as seq(m+1) = exp(-j*pi*R*m*(m+1)/N) for m = 0,...,N-1.', 'R and N must be coprime (gcd(R,N)=1). Zadoff-Chu sequences have constant amplitude and ideal periodic auto-correlation.'], seealso: [] },
    gen2par: { summary: 'Convert between parity-check and generator matrices', syntax: ['H = gen2par(G)', 'G = gen2par(H)'], description: ['H = gen2par(G) converts a standard-form binary generator matrix G to the corresponding parity-check matrix H.', 'G = gen2par(H) converts a standard-form binary parity-check matrix H to the corresponding generator matrix G.', 'Both matrices must be in standard form: G = [I_k | P] and H = [P\' | I_(n-k)].'], seealso: ['cyclgen', 'hammgen'] },
    gfweight: { summary: 'Minimum distance of a linear block code', syntax: ['wt = gfweight(genmat)', 'wt = gfweight(genmat,\'gen\')', 'wt = gfweight(parmat,\'par\')', 'wt = gfweight(genpoly,n)'], description: ['wt = gfweight(genmat) returns the minimum distance of the linear block code whose generator matrix is genmat.', 'wt = gfweight(parmat,\'par\') returns the minimum distance using the parity-check matrix.', 'wt = gfweight(genpoly,n) returns the minimum distance for a cyclic code of codeword length n with generator polynomial genpoly.'], seealso: ['hammgen', 'cyclpoly', 'bchgenpoly'] },
    gfadd: { summary: 'Adds two GF(2) polynomials, a and b.', syntax: ['c = gfadd(a,b)', 'c = gfadd(a,b,p)', 'c = gfadd(a,b,p,len)', 'c = gfadd(a,b,field)'], seealso: ['gfsub', 'gfconv', 'gfmul', 'gfdeconv', 'gfdiv'], description: ['c = gfadd(a,b) adds two GF(2) polynomials, a and b.', 'c = gfadd(a,b,p) adds two GF(p) polynomials, a and b.', 'c = gfadd(a,b,p,len) adds row vectors a and b and returns a row vector of length len.', 'c = gfadd(a,b,field) adds two GF(pm) elements.', 'Note'] },
    gfdiv: { summary: 'Divides b by a in GF(2) element-by-element.', syntax: ['quot = gfdiv(b,a)', 'quot = gfdiv(b,a,p)', 'quot = gfdiv(b,a,field)'], seealso: ['gfmul', 'gfdeconv', 'gfconv', 'gftuple'], description: ['quot = gfdiv(b,a) divides b by a in GF(2) element-by-element. Each entry in a and b represents an element of GF(2).', 'quot = gfdiv(b,a,p) divides b by a in GF(p) and returns the quotient. p is a prime number. If a and b are matrices of the same size, the function treats each element independently. All entries of b, a, and quot are between 0 and p-1.', 'quot = gfdiv(b,a,field) divides b by a in GF(pm) and returns the quotient. p is a prime number and m is a positive integer. If a and b are matrices of the same size, then the function treats each element independently. All entries of b, a, and quot are the exponential formats of elements of GF(pm) relative to some primitive element of GF(pm). field is the matrix listing all elements of GF(pm), arranged relative to the same primitive element. See Representing Elements of Galois Fields for an explanation of these formats.'] },
    cyclpoly: { summary: 'Returns the row vector representing one nontrivial generator polynomial for a cyclic code with codeword length N and message length K.', syntax: ['pol = cyclpoly(N,K)', 'pol = cyclpoly(N,K,opt)'], seealso: ['cyclgen', 'encode', 'gfprimfd'], description: ['pol = cyclpoly(N,K) returns the row vector representing one nontrivial generator polynomial for a cyclic code with codeword length N and message length K.', 'pol = cyclpoly(N,K,opt) searches for one or more nontrivial generator polynomials for cyclic codes with codeword length N and message length K. The output pol depends on the value of opt, which specifies the weight of the polynomial.'] },
    oct2poly: { summary: 'Convert octal number to binary polynomial coefficients', syntax: ['p = oct2poly(oct)', 'p = oct2poly(oct,ord)'], seealso: ['poly2trellis', 'oct2dec'], description: ['b = oct2poly(oct) converts an octal number, oct, to a vector of binary coefficients, b.', 'b = oct2poly(oct,ord) specifies the power order, ord, of the coefficients that comprise the output. If omitted, ord is \'descending\'.'] },
    fspl: { summary: 'Free space path loss', syntax: ['L = fspl(R,lambda)'], seealso: ['fogpl', 'gaspl', 'rainpl'], description: ['L = fspl(R,lambda) returns the free space path loss in decibels for a waveform with wavelength lambda propagated over a distance of R meters. The minimum value of L is zero, indicating no path loss.'] },
    cyclgen: { summary: 'Produce parity-check and generator matrices for a cyclic code', syntax: ['h = cyclgen(n,p)', 'h = cyclgen(n,p,opt)', '[h,g] = cyclgen(___)', '[h,g,k] = cyclgen(___)'], description: ['h = cyclgen(n,p) produces an (n-k)-by-n parity-check matrix for a systematic binary cyclic code of codeword length n with generator polynomial p.', '[h,g] = cyclgen(...) also returns the k-by-n generator matrix g corresponding to h.', '[h,g,k] = cyclgen(...) additionally returns k, the message length of the code.'], seealso: ['encode', 'decode', 'bchgenpoly', 'cyclpoly'] },
    qfunc: { summary: 'Q function (Gaussian tail probability)', syntax: ['y = qfunc(x)'], description: ['y = qfunc(x) returns the Q function value for each element of real-valued x.', 'The Q function is Q(x) = (1/sqrt(2*pi)) * integral from x to Inf of exp(-t^2/2) dt.', 'It equals 0.5*erfc(x/sqrt(2)) and represents the probability that a standard normal random variable exceeds x.'], seealso: ['qfuncinv', 'erfc'] },
    quantiz: { summary: 'Produce a quantization index and quantized output value', syntax: ['index = quantiz(sig,partition)', '[index,quants] = quantiz(sig,partition,codebook)', '[index,quants,distor] = quantiz(sig,partition,codebook)'], description: ['index = quantiz(sig,partition) returns quantization indices for sig using the scalar quantization boundary vector partition.', '[index,quants,distor] = quantiz(sig,partition,codebook) also maps indices through codebook to get quantized values and returns mean-square distortion.', 'partition is a length-(n-1) vector defining n quantization regions; codebook has n values, one per region.'], seealso: ['lloyds'] },
    qfuncinv: { summary: 'Inverse Q function', syntax: ['z = qfuncinv(y)'], description: ['z = qfuncinv(y) returns x such that qfunc(x) = y.', 'It is the inverse of the Gaussian tail probability function, equivalent to sqrt(2)*erfcinv(2*y).', 'y must be in (0,1); qfuncinv(0.5) = 0, qfuncinv(1) = -Inf, qfuncinv(0) = Inf.'], seealso: ['qfunc', 'erfinv'] },
    oct2dec: { summary: 'Convert octal to decimal numbers', syntax: ['dec = oct2dec(oct)'], description: ['dec = oct2dec(oct) converts octal numbers in matrix oct to decimal numbers, element by element.', 'The rightmost digit is the least significant digit; for example, oct2dec(144) = 100.', 'Elements of oct must be nonnegative integers whose decimal value fits in a double.'], seealso: [] },
    vec2mat: { summary: 'Convert vector into matrix (row-major, padded)', syntax: ['mat = vec2mat(vec,matcol)', 'mat = vec2mat(vec,matcol,padding)'], description: ['mat = vec2mat(vec,matcol) reshapes the vector vec into a matrix with matcol columns, filling row-major (left to right, top to bottom).', 'If length(vec) is not divisible by matcol, the last row is zero-padded by default.', 'mat = vec2mat(vec,matcol,padding) pads with the scalar value padding instead of zero.'], seealso: [] },
    compand: { summary: 'Source code mu-law or A-law compressor or expander', syntax: ['out = compand(in,param,v)', 'out = compand(in,param,v,method)'], description: ['out = compand(in,param,v) performs mu-law compression with parameter param on input in, scaled to peak value v.', 'method can be \'mu/compressor\', \'mu/expander\', \'A/compressor\', or \'A/expander\'.', 'Companding improves quantization SNR for speech signals by applying logarithmic compression before uniform quantization.'], seealso: [] },
    de2bi: { summary: 'Convert decimal numbers to binary digits', syntax: ['b = de2bi(d)', 'b = de2bi(d,n)', 'b = de2bi(d,n,base)', 'b = de2bi(d,[],\'left-msb\')'], description: ['b = de2bi(d) converts nonneg integers d to a binary matrix with LSB in first column.', 'b = de2bi(d,n) forces n columns (zero-padded or truncated).', 'de2bi(d,[],"left-msb") puts the MSB in the first column (standard binary order).'], seealso: ['bi2de'] },
    bi2de: { summary: 'Convert binary digits to decimal numbers', syntax: ['d = bi2de(b)', 'd = bi2de(b,base)', 'd = bi2de(b,\'left-msb\')'], description: ['d = bi2de(b) converts each row of binary matrix b to the corresponding decimal integer, with LSB in first column.', 'd = bi2de(b,"left-msb") treats the first column as the MSB (standard binary order).', 'd = bi2de(b,base) interprets rows as base-p numbers.'], seealso: ['de2bi'] },
    symerr: { summary: 'Count symbol errors and compute symbol error rate', syntax: ['[number,ratio] = symerr(x,y)', '[number,ratio] = symerr(x,y,flg)', '[number,ratio,individual] = symerr(x,y)'], description: ['[number,ratio] = symerr(x,y) counts the number of positions where x and y differ and returns the symbol error rate ratio.', 'flg can be \'overall\', \'column-wise\', or \'row-wise\' to select the comparison dimension.', '[number,ratio,individual] = symerr(...) also returns a binary matrix marking differing positions.'], seealso: ['biterr', 'alignsignals', 'finddelay'] },
    biterr: { summary: 'Count bit errors and compute bit error rate', syntax: ['[number,ratio] = biterr(x,y)', '[number,ratio] = biterr(x,y,k)', '[number,ratio] = biterr(x,y,k,flag)', '[number,ratio,individual] = biterr(___)'], description: ['[number,ratio] = biterr(x,y) compares unsigned binary representations of x and y and counts differing bits.', 'k specifies the number of bits per symbol (default: ceil(log2(max(max(x),max(y))+1))).', 'flag selects the comparison mode: \'overall\' (default), \'column-wise\', or \'row-wise\'.'], seealso: ['symerr', 'alignsignals', 'finddelay'] },
    bin2gray: { summary: 'Convert positive integers to Gray-encoded integers', syntax: ['code = bin2gray(x)'], description: ['code = bin2gray(x) converts each nonneg integer in x to its Gray code equivalent.', 'Gray code sequences differ in exactly one bit between consecutive integers, making them useful for symbol mapping in PSK/QAM.', 'The inverse operation is gray2bin.'], seealso: ['gray2bin'] },
    gray2bin: { summary: 'Convert Gray-encoded integers to positive integers', syntax: ['code = gray2bin(x)'], description: ['code = gray2bin(x) converts each Gray-coded integer in x to the corresponding standard binary integer.', 'It is the inverse of bin2gray and is used to decode Gray-mapped constellation symbols.'], seealso: ['bin2gray'] },
    qammod: { summary: 'Quadrature amplitude modulation', syntax: ['Y = qammod(X,M)', 'Y = qammod(X,M,symOrder)', 'Y = qammod(___,Name=Value)'], description: ['Y = qammod(X,M) modulates input integers X using M-QAM, returning complex baseband symbols.', 'M must be a power of 4 for square QAM (e.g. 4, 16, 64, 256). symOrder can be \'gray\' (default), \'bin\', or a custom order vector.', 'Name=Value options include InputType (\'integer\' or \'bit\'), UnitAveragePower (logical), and PlotConstellation.'], seealso: ['qamdemod', 'pskmod', 'pammod'] },
    qamdemod: { summary: 'Quadrature amplitude demodulation', syntax: ['Z = qamdemod(Y,M)', 'Z = qamdemod(Y,M,symOrder)', 'Z = qamdemod(___,Name=Value)'], description: ['Z = qamdemod(Y,M) demodulates complex QAM signal Y with modulation order M, returning integer symbol indices.', 'symOrder and Name=Value options mirror qammod; OutputType can be \'integer\' or \'bit\'.', 'Decision regions use minimum Euclidean distance to the constellation points.'], seealso: ['qammod', 'pskdemod', 'pamdemod'] },
    pskmod: { summary: 'Phase shift keying modulation', syntax: ['Y = pskmod(X,M)', 'Y = pskmod(X,M,phaseoffset)', 'Y = pskmod(X,M,phaseoffset,symorder)'], description: ['Y = pskmod(X,M) maps integer symbols X in [0,M-1] to M evenly-spaced points on the unit circle.', 'phaseoffset shifts the constellation (default 0 for BPSK/QPSK, pi/4 for pi/4-QPSK).', 'symorder is \'gray\' (default), \'bin\', or a custom permutation vector.'], seealso: ['pskdemod', 'qammod', 'dpskmod'] },
    pskdemod: { summary: 'Phase shift keying demodulation', syntax: ['Z = pskdemod(Y,M)', 'Z = pskdemod(Y,M,phaseoffset)', 'Z = pskdemod(Y,M,phaseoffset,symorder)'], description: ['Z = pskdemod(Y,M) demodulates complex PSK signal Y to integer symbols in [0,M-1].', 'phaseoffset and symorder must match the modulator settings used to create Y.', 'Demodulation uses hard minimum-distance decisions; name-value options add noise variance for soft decisions.'], seealso: ['pskmod', 'qamdemod', 'dpskdemod'] },
    marcumq: { summary: 'Generalized Marcum Q-function', syntax: ['Q = marcumq(a,b)', 'Q = marcumq(a,b,m)'], description: ['Q = marcumq(a,b) computes the Marcum Q-function of order 1 for noncentrality parameter a and argument b.', 'Q = marcumq(a,b,m) computes the generalized Marcum Q-function of order m.', 'Used to compute error probabilities for noncoherent detection of signals in AWGN (e.g. Rician fading BER).'], seealso: ['besseli'] },
    finddelay: { summary: 'Estimate delay between signals', syntax: ['d = finddelay(x,y)', 'd = finddelay(x,y,maxlag)'], description: ['d = finddelay(x,y) returns the estimated delay d between signals x and y using cross-correlation.', 'A positive d means y is delayed with respect to x; negative means x is delayed.', 'maxlag limits the search range; the default is max(length(x),length(y))-1.'], seealso: ['alignsignals', 'xcorr'] },
    dpskmod: { summary: 'Differential phase shift keying modulation', syntax: ['y = dpskmod(x,M)', 'y = dpskmod(x,M,phaserot)', 'y = dpskmod(x,M,phaserot,symorder)'], description: ['y = dpskmod(x,M) modulates integer symbols x using M-DPSK, encoding information in phase differences between successive symbols.', 'phaserot sets the phase rotation per symbol (default pi/M).', 'DPSK is more robust than coherent PSK because it does not require a phase reference at the receiver.'], seealso: ['dpskdemod', 'pskmod', 'pskdemod'] },
    dpskdemod: { summary: 'Differential phase shift keying demodulation', syntax: ['z = dpskdemod(y,M)', 'z = dpskdemod(y,M,phaserot)', 'z = dpskdemod(y,M,phaserot,symorder)'], description: ['z = dpskdemod(y,M) demodulates M-DPSK signal y by computing phase differences between successive symbols.', 'phaserot and symorder must match the dpskmod settings used to produce y.', 'Returns integer symbols in [0,M-1].'], seealso: ['dpskmod', 'pskdemod', 'pskmod'] },
    poly2trellis: { summary: 'Convert convolutional code polynomials to trellis description', syntax: ['trellis = poly2trellis(ConstraintLength,CodeGenerator)', 'trellis = poly2trellis(ConstraintLength,CodeGenerator,FeedbackConnection)'], description: ['trellis = poly2trellis(ConstraintLength,CodeGenerator) builds the trellis structure for a rate-K/N convolutional encoder specified by constraint lengths and octal generator polynomials.', 'The output trellis is a struct with fields numInputSymbols, numOutputSymbols, numStates, nextStates, and outputs.', 'Pass the result to convenc (encoder) or vitdec (Viterbi decoder).'], seealso: ['convenc', 'istrellis'] },
    convenc: { summary: 'Convolutionally encode binary data', syntax: ['codedout = convenc(msg,trellis)', 'codedout = convenc(msg,trellis,puncpat)', '[codedout,fstate] = convenc(___,istate)'], description: ['codedout = convenc(msg,trellis) encodes the binary vector msg using the convolutional encoder described by the trellis structure.', 'puncpat is a puncture pattern (binary vector) to achieve a higher code rate.', 'istate sets the encoder starting state; fstate returns the final state, allowing block-by-block encoding.'], seealso: ['poly2trellis', 'vitdec', 'istrellis'] },
    istrellis: { summary: 'Check if input is a valid trellis structure', syntax: ['[isok,status] = istrellis(s)'], description: ['[isok,status] = istrellis(s) checks if s is a valid trellis structure usable by convenc and vitdec.', 'isok is 1 if valid, 0 otherwise. status is an empty string if valid, or a message describing the problem.', 'A valid trellis has integer fields numInputSymbols, numOutputSymbols, numStates, and matrices nextStates and outputs.'], seealso: ['poly2trellis', 'convenc'] },
    hammgen: { summary: 'Produce parity-check and generator matrices for Hamming code', syntax: ['h = hammgen(m)', 'h = hammgen(m,poly)', '[h,g] = hammgen(___)', '[h,g,n,k] = hammgen(___)'], description: ['h = hammgen(m) returns the m-by-(2^m-1) parity-check matrix for a binary Hamming code with codeword length n = 2^m-1.', '[h,g] = hammgen(m) also returns the k-by-n generator matrix g, where k = n-m.', '[h,g,n,k] = hammgen(m) additionally returns the codeword length n and message length k.'], seealso: ['encode', 'decode', 'gen2par'] },
    primpoly: { summary: 'Find primitive polynomials for a Galois field', syntax: ['pr = primpoly(m)', 'pr = primpoly(m,opt)', 'pr = primpoly(___,"nodisplay")'], description: ['pr = primpoly(m) returns the default primitive polynomial for GF(2^m) as a decimal integer (coefficient bit-mask).', 'opt can be \'min\', \'max\', \'all\', or \'all+p\' to select which primitive polynomials are returned.', '"nodisplay" suppresses the polynomial printout; the numeric result is unaffected.'], seealso: ['gf', 'isprimitive'] },
    gfminpol: { summary: 'Find the minimal polynomial of an element of a Galois field', syntax: ['pol = gfminpol(k,m)', 'pol = gfminpol(k,m,p)', 'pol = gfminpol(k,prim_poly,p)'], description: ['pol = gfminpol(k,m) returns the minimal polynomial of alpha^k over GF(2), where alpha is a root of the default primitive polynomial for GF(2^m).', 'If k is a vector, pol is a matrix with one row per element.', 'The polynomial is represented as a row vector of GF(2) coefficients in ascending power order.'], seealso: ['gfprimdf', 'gfcosets', 'gfroots'] },
    gftrunc: { summary: 'Minimize the length of a polynomial representation over GF', syntax: ['c = gftrunc(a)'], description: ['c = gftrunc(a) removes trailing (high-order) zeros from the row-vector representation of a polynomial over a Galois field.', 'If the polynomial has degree d, trailing zeros beyond position d+1 are removed.', 'Middle or leading zeros are preserved since they represent non-zero intermediate coefficients.'], seealso: ['gfadd', 'gfsub', 'gfconv', 'gfdeconv'] },
    iqimbal2coef: { summary: 'Convert I/Q imbalance to compensator coefficient', syntax: ['c = iqimbal2coef(ampImbalanceDB,phaseImbalanceDeg)'], seealso: ['iqcoef2imbal'], description: ['C = iqimbal2coef(A,P) converts an I/Q amplitude and phase imbalance to its equivalent compensator coefficient.'] },
    rsgenpolycoeffs: { summary: 'Generator polynomial coefficients of a Reed-Solomon code', syntax: ['g = rsgenpolycoeffs(n,k)'], seealso: ['rsgenpoly', 'rsenc'], description: ['x = rsgenpolycoeffs(N,K) returns the coefficients for the generator polynomial of an [N,K] Reed-Solomon code.', '[x,t] = rsgenpolycoeffs(___) returns the error-correction capability of the code.'] },
    iqcoef2imbal: { summary: 'Convert compensator coefficient to amplitude and phase imbalance', syntax: ['[A,P] = iqcoef2imbal(C)'], description: ['[A,P] = iqcoef2imbal(C) converts a complex IQ imbalance compensator coefficient C to its equivalent amplitude imbalance A (dB) and phase imbalance P (degrees).', 'C is typically the output of the step function of an IQImbalanceCompensator System object.', 'Use imbal2iqcoef to convert in the opposite direction.'], seealso: [] },
    fmmod: { summary: 'Frequency modulation', syntax: ['y = fmmod(x,Fc,Fs,freqdev)', 'y = fmmod(x,Fc,Fs,freqdev,ini_phase)'], description: ['y = fmmod(x,Fc,Fs,freqdev) returns the FM-modulated signal y for message x with carrier frequency Fc (Hz), sample rate Fs (Hz), and frequency deviation freqdev (Hz).', 'Requires Fs >= 2*Fc and freqdev < Fc.', 'ini_phase sets the initial phase of the carrier in radians (default 0).'], seealso: ['fmdemod', 'ammod', 'pmmod'] },
    matdeintrlv: { summary: 'Restore ordering of symbols using a matrix interleaver', syntax: ['deintrlvd = matdeintrlv(data,Nrows,Ncols)'], description: ['deintrlvd = matdeintrlv(data,Nrows,Ncols) is the inverse of matintrlv: it fills an Nrows-by-Ncols matrix column-by-column with the elements of data, then reads out the rows sequentially.', 'data must have length Nrows*Ncols.', 'Interleaving spreads burst errors across multiple codewords to improve error-correction performance.'], seealso: ['matintrlv', 'algdeintrlv'] },
    algdeintrlv: { summary: 'Restore ordering of symbols using an algebraically derived interleaver', syntax: ['deintrlvd = algdeintrlv(data,N,\'takeshita-costello\',c,a)', 'deintrlvd = algdeintrlv(data,N,\'welch-costas\',a)'], description: ['algdeintrlv undoes the permutation applied by algintrlv, using the same algebraic parameters.', 'The Takeshita-Costello and Welch-Costas interleavers are deterministic and hardware-friendly, avoiding the need to store a full permutation table.', 'data must have length N; the permutation table is recomputed from the parameters.'], seealso: ['algintrlv', 'matdeintrlv'] },
  };
