// Help entries for the Econometrics Toolbox, extracted from econ.ts.
// Keyed by function name; consumed via ToolboxModule.help (see tb/types.ts).
import type { HelpEntry } from './types';

export const HELP_ECON: Record<string, HelpEntry | string> = {
    adftest: { summary: 'Returns the rejection decision from conducting an augmented Dickey-Fuller test for a unit root in the input univariate time series.', syntax: ['h = adftest(y)', 'StatTbl = adftest(Tbl)', '[ ___ ] = adftest( ___ ,Name=Value)', '[ ___ ,reg] = adftest( ___ )'], seealso: ['kpsstest', 'lmctest', 'pptest', 'vratiotest', 'i10test'], description: ['h = adftest(y) returns the rejection decision from conducting an augmented Dickey-Fuller test for a unit root in the input univariate time series.', '[h,pValue,stat,cValue] = adftest(y) also returns the _p_ -value pValue, test statistic stat, and critical value cValue of the test.', 'StatTbl = adftest(Tbl) returns a table containing variables for the test results, statistics, and settings from conducting an augmented Dickey-Fuller test for a unit root in the last variable of the input table or timetable. To select a different variable to test, use the DataVariable name-value argument.', '[___] = adftest(___,Name=Value) specifies options using one or more name- value arguments in addition to any of the input argument combinations in previous syntaxes. adftest returns the output argument combination for the corresponding input arguments.', 'Some options control the number of tests to conduct. The following conditions apply when adftest conducts multiple tests:'] },
    pptest: { summary: 'Returns the rejection decision from conducting the Phillips-Perron test for a unit root in the input univariate time series.', syntax: ['h = pptest(y)', 'StatTbl = pptest(Tbl)', '[ ___ ] = pptest( ___ ,Name=Value)', '[ ___ ,reg] = pptest( ___ )'], seealso: ['adftest', 'kpsstest', 'vratiotest', 'lmctest'], description: ['h = pptest(y) returns the rejection decision from conducting the Phillips-Perron test for a unit root in the input univariate time series.', '[h,pValue,stat,cValue] = pptest(y) also returns the _p_ -value pValue, test statistic stat, and critical value cValue of the test.', 'StatTbl = pptest(Tbl) returns a table containing variables for the test results, statistics, and settings from conducting the Phillips-Perron test on the last variable of the input table or timetable. To select a different variable to test, use the DataVariable name-value argument.', '[___] = pptest(___,Name=Value) specifies options using one or more name- value arguments in addition to any of the input argument combinations in previous syntaxes. pptest returns the output argument combination for the corresponding input arguments.', 'Some options control the number of tests to conduct. The following conditions apply when pptest conducts multiple tests:'] },
    price2ret: { summary: 'Returns the matrix of numVars continuously compounded return series, and corresponding time intervals, from the input matrix of numVars price series.', syntax: ['[Returns,intervals] = price2ret(Prices)', 'ReturnTbl = price2ret(PriceTbl)', '[ ___ ] = price2ret( ___ ,Name=Value)'], seealso: ['ret2price', 'tick2ret'], description: ['[Returns,intervals] = price2ret(Prices) returns the matrix of numVars continuously compounded return series, and corresponding time intervals, from the input matrix of numVars price series.', 'ReturnTbl = price2ret(PriceTbl) returns a table or timetable of continuously compounded return series of each variable in the input table or timetable of price series. To select different variables from which to compute returns, use the DataVariables name-value argument._(since R2022a)_', '[___] = price2ret(___,Name=Value) specifies options using one or more name-value arguments in addition to any of the input argument combinations in previous syntaxes. price2ret returns the output argument combination for the corresponding input arguments. For example, price2ret(Tbl,Method="periodic",DataVariables=1:5) computes the simple periodic returns of the first five variables in the input table Tbl._(since R2022a)_'] },
    tick2ret: { summary: 'Convert tick (price) series to return series', syntax: ['ret = tick2ret(price)', 'ret = tick2ret(price,base)'], seealso: ['ret2tick', 'price2ret'] },
    lagmatrix: { summary: 'Shifts the input regular series in time by the input vector of lags (positive) or leads (negative), and returns the matrix of shifted series.', syntax: ['YLag = lagmatrix(Y,lags)', '[YLag,TLag] = lagmatrix(Y,lags)', 'LagTbl = lagmatrix(Tbl,lags)', '[ ___ ] = lagmatrix( ___ ,Name=Value)'], description: ['YLag = lagmatrix(Y,lags) shifts the input regular series in time by the input vector of lags (positive) or leads (negative), and returns the matrix of shifted series.', '[YLag,TLag] = lagmatrix(Y,lags) also returns a vector representing the common time base for the shifted series relative to the original time base of 1, 2, 3, …, numObs.', 'LagTbl = lagmatrix(Tbl,lags) shifts all variables in the input table or timetable, which represent regular time series, and returns a table or timetable of shifted series. To select different variables to shift, use the DataVariables name-value argument._(since R2022a)_', '[___] = lagmatrix(___,Name=Value) specifies options using one or more name-value arguments in addition to any of the input argument combinations in previous syntaxes. lagmatrix returns the output argument combination for the corresponding input arguments. For example, lagmatrix(Tbl,1,Y0=zeros(1,5),DataVariables=1:5) lags, by one period, the first five variables in the input table Tbl and sets the presample of each series to 0.'] },
    autocorr: { summary: 'Returns the sample autocorrelation function (ACF) and associated lags of the input univariate time series.', syntax: ['[acf,lags] = autocorr(y)', 'ACFTbl = autocorr(Tbl)', '[ ___ ] = autocorr( ___ ,Name=Value)', 'autocorr( ___ )'], description: ['[acf,lags] = autocorr(y) returns the sample autocorrelation function (ACF) and associated lags of the input univariate time series.', 'ACFTbl = autocorr(Tbl) returns a table containing variables for the sample ACF and associated lags of the last variable in the input table or timetable. To select a different variable for which to compute the ACF, use the DataVariable name-value argument._(since R2022a)_', '[___,bounds] = autocorr(___) uses any input-argument combination in the previous syntaxes, and returns the output-argument combination for the corresponding input arguments and the approximate upper and lower confidence bounds on the ACF.', '[___] = autocorr(___,Name=Value) uses additional options specified by one or more name-value arguments. For example, autocorr(Tbl,DataVariable="RGDP",NumLags=10,NumSTD=1.96) returns 10 lags of the sample ACF of the table variable "RGDP" in Tbl and 95% confidence bounds.', 'autocorr(___) plots the sample ACF of the input series with confidence bounds.'] },
  };
